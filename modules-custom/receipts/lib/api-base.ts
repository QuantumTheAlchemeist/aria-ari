// PHASE 0: the ONE place the API base path is defined.
// All client components and curl commands derive their URLs from here.
// Change this single line if the module is mounted at a different prefix.
export const API_BASE = "/api/aria";
